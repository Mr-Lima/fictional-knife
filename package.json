{
  "name": "fictional-knife",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src/ --exec \"npm run compile-dev\" -e ts",
    "compile-dev": "ts-node --project tsconfig.json src/server.ts",
    "compile": "tsc",
    "build": "npm run wotan && rm -rf ./dist/* && npm run compile",
    "prod": "npm run build && node ./dist/server.js"
  },
  "author": "aldev",
  "license": "ISC",
  "devDependencies": {
    "@fimbul/mithotyn": "^0.18.0",
    "@fimbul/wotan": "^0.20.0",
    "nodemon": "^1.18.11",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.2"
  },
  "dependencies": {
    "dotenv": "^7.0.0"
  }
}
